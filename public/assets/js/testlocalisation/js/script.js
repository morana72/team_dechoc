$(document).ready(function() {

	// recupere les données


/*

	
	// ecrire les données dans le json.json
	
	var $homeMobileCountryCode = '';
	var $homeMobileNetworkCode = '';
	var $radioType = '" "';
	var $carrier = '" "';
	var $considerIp = '" "';
	var $cellId = '';
	var $locationAreaCode = '';
	var $mobileCountryCode = '';
	var $mobileNetworkCode = '';
	var $age = '';
	var $signalStrength = '';
	var $timingAdvance = '';
	var $macAddress = '" "';
	var $signalStrength = '';
	var $age1 = '';
	var $channel = '';
	var $signalToNoiseRatio = '';

		$.ajax({
       url : 'js/Traitement.php', // La ressource ciblée
       type : 'POST', // Le type de la requête HTTP.
       data :{  homeMobileCountryCode : $homeMobileCountryCode,
			    homeMobileNetworkCode : $homeMobileNetworkCode,
				radioType : $radioType,
				carrier : $carrier,
				considerIp : $considerIp,
				cellId : $cellId,
				locationAreaCode : $locationAreaCode,
				mobileCountryCode : $mobileCountryCode,
				mobileNetworkCode : $mobileNetworkCode,
				age : $age,
				signalStrength : $signalStrength,
				timingAdvance : $timingAdvance,
				macAddress : $macAddress,
				signalStrength : $signalStrength,
				age1 : $age1,
				channel : $channel,
				signalToNoiseRatio : $signalToNoiseRatio
			},
       success: function(){ 
            alert('Success!');
       }

    });


	$.ajax({
       url : 'https://www.googleapis.com/geolocation/v1/geolocate?key=AIzaSyCHFlhpfLm2czQKHRbj0dci-09eIPtVKpE', // La ressource ciblée
       type : 'POST', // Le type de la requête HTTP.
       data : 'js/json.json'
    });*/

	// vider les données du json.json

});